name: mobile_app_inventory_system
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1  # Stable, compatible
  flutter_hooks: ^0.21.3+1
  hooks_riverpod: ^2.6.1  # Match with flutter_riverpod
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  freezed_annotation: ^3.1.0
  cloud_firestore: ^6.0.3
  get_it: ^8.2.0
  freezed: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  custom_lint: ^0.8.0
  build_runner: ^2.7.1  # Downgrade para resolver analyzer/test conflict

dependency_overrides:
  analyzer: ^6.0.0

flutter:
  uses-material-design: true